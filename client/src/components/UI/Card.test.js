import React from 'react';
import { render } from '@testing-library/react';
import { CardPost } from './Card.js';

describe('Card post', () => {
    test('always true', () => {
        expect(true).toBe(true);
    });
});